namespace Ex04.Menus.Delegates
{
    internal class ExitItem : MenuItem // Exit Represents an item in the menu - if primMenu : title = exit else title = back
    {
        public ExitItem(string i_Title): base(i_Title)
        {
        }
    }
}
